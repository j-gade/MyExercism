#if !defined(HAMMING_H)
#define HAMMING_H

#include <string>

namespace hamming {

    int compute(std::string const & first_seq, std::string const & second_seq);
    
}  // namespace hamming

#endif // HAMMING_H